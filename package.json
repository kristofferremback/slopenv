{
  "name": "slopenv",
  "version": "0.6.0",
  "description": "direnv, but the config lives outside the repo and secrets live in the OS secret store",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "bun build --compile --minify --bytecode src/cli.ts --outfile slopenv",
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.6.0"
  }
}
